import { describe, it, expect } from 'vitest';

// Placeholder E2E spec. Replace with Playwright or Puppeteer based runner when wiring CI.
describe('popup smoke test', () => {
  it('placeholder passes', () => {
    expect(true).toBe(true);
  });
});
